# 🖥️ Система учета ТМЦ (Товарно-Материальных Ценностей)

**Flask-приложение для учета, добавления, редактирования и удаления ТМЦ с поддержкой фото, привязки к отделам, группам и производителям.**

> 📌 **Примечание:** Flask проект обратно совместим с родительским [webuseorg3](https://github.com/donpadlo/webuseorg3), переписан на свой лад.

---

## 📑 Содержание

- [Основные возможности](#-основные-возможности)
- [Требования](#-требования)
- [Установка и настройка](#️-установка-и-настройка)
  - [Клонирование проекта](#1-клонирование-проекта)
  - [Виртуальное окружение](#2-создание-виртуального-окружения-и-установка-зависимостей)
  - [База данных](#3-настройка-базы-данных-mysql)
  - [Переменные окружения](#4-настройка-переменных-окружения)
  - [Папка для фотографий](#5-настройка-папки-для-фотографий)
  - [Nginx](#6-настройка-nginx-для-раздачи-фото)
  - [Запуск](#7-запуск-приложения)
- [Функциональность](#-функциональность)
- [Структура проекта](#-структура-проекта)
- [Полезные команды](#-полезные-команды)
- [Безопасность](#️-безопасность)

---

## 📌 Основные возможности

### 🔧 Базовые операции
- ✅ **Добавление, редактирование, удаление ТМЦ**
- 📸 **Загрузка фотографий** (хранение в `/var/www/html/photos`, отображение через Nginx)
- 🔍 **Фильтрация списка ТМЦ** по отделам на главной странице

### 🏢 Организационная структура
- 🧑‍💼 **Привязка к организации**, месту нахождения, ответственному пользователю
- 🏢 **Привязка ТМЦ к отделам** (Отдел связи, ОТО, ПС и др.)
- 👥 **Управление пользователями** с различными ролями (Админ, МОЛ, Пользователь)

### 🧩 Классификация ТМЦ
- 🧩 **Динамический выбор:** Группа → Производитель → Наименование
- ➕ **Создание нового наименования** прямо из формы
- 🔗 **Составные ТМЦ** с привязкой комплектующих
- 📦 **Управление комплектующими** для составных ТМЦ

### 📊 Учет и отчетность
- 📈 **Статистика** по количеству и стоимости ТМЦ
- 🔄 **История перемещений** между МОЛ и пользователями
- 📝 **История комментариев** к ТМЦ
- 📋 **Временная выдача ТМЦ** пользователям

### 🔐 Безопасность и доступ
- 🔒 **Система ролей** (Администратор, МОЛ, Пользователь)
- 👤 **Персональные профили** с возможностью редактирования
- 🔐 **Аутентификация** через Flask-Login

---

## 🚀 Требования

| Компонент | Версия | Назначение |
|-----------|--------|------------|
| **Python** | 3.8+ | Основной язык разработки |
| **MySQL** | 8.0+ | База данных |
| **Nginx** | - | Веб-сервер и раздача статики |
| **ОС** | Linux (Ubuntu 20.04+) | Рекомендуемая операционная система |

---

## 🛠️ Установка и настройка

### 1. Клонирование проекта

```bash
git clone https://github.com/ваш-логин/flask-tmc-app.git
cd flask-tmc-app
```

> 💡 **Совет:** Если репозиторий ещё не создан, инициализируйте локально:
> ```bash
> git init
> git remote add origin https://github.com/ваш-логин/flask-tmc-app.git
> ```

---

### 2. Создание виртуального окружения и установка зависимостей

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация окружения
source venv/bin/activate

# Установка зависимостей
pip install Flask Flask-SQLAlchemy PyMySQL python-dotenv cryptography
```

> 📝 **Примечание:** Для продакшена рекомендуется использовать `requirements.txt`:
> ```bash
> pip install -r requirements.txt
> ```

---

### 3. Настройка базы данных MySQL

#### ✅ Создание базы и таблиц

Выполните следующие SQL-команды:

```sql
-- Создание базы данных
CREATE DATABASE webuseorg3 CHARACTER SET utf8mb3 COLLATE utf8mb3_bin;

USE webuseorg3;

-- Таблица отделов
CREATE TABLE `department` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(255) NOT NULL,
  `code` VARCHAR(50) NOT NULL COMMENT 'Короткий код (например, ОС, ОТО)',
  `active` TINYINT(1) NOT NULL DEFAULT 1,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb3 COLLATE=utf8mb3_bin;

-- Добавление столбца department_id в equipment
ALTER TABLE `equipment`
ADD COLUMN `department_id` INT NULL DEFAULT NULL AFTER `tmcgo`,
ADD INDEX `fk_equipment_department_idx` (`department_id` ASC);

ALTER TABLE `equipment`
ADD CONSTRAINT `fk_equipment_department`
  FOREIGN KEY (`department_id`)
  REFERENCES `department` (`id`)
  ON DELETE SET NULL
  ON UPDATE CASCADE;

-- Заполнение отделов
INSERT INTO `department` (`name`, `code`, `active`) VALUES
('Отдел связи', 'ОС', 1),
('Отдел технического обеспечения', 'ОТО', 1),
('Противопожарная служба', 'ПС', 1);

-- Привязка существующих ТМЦ (пример)
UPDATE `equipment` SET `department_id` = 1 WHERE `id` BETWEEN 400 AND 439;
UPDATE `equipment` SET `department_id` = 2 WHERE `id` BETWEEN 440 AND 445;
```

> ⚠️ **Важно:** Убедитесь, что все необходимые таблицы созданы согласно схеме базы данных.

---

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Секретный ключ для Flask (сгенерируйте случайную строку)
SECRET_KEY=your_secret_key_here

# URL подключения к базе данных
DATABASE_URL=mysql+pymysql://root:your_password@localhost/webuseorg3
```

> ⚠️ **Безопасность:** 
> - Замените `your_password` на реальный пароль MySQL
> - Никогда не коммитьте `.env` в репозиторий
> - Добавьте `.env` в `.gitignore`

**Генерация секретного ключа:**
```bash
python3 -c "import secrets; print(secrets.token_hex(32))"
```

---

### 5. Настройка папки для фотографий

```bash
# Создание директории
sudo mkdir -p /var/www/html/photos

# Установка прав доступа
sudo chown -R $USER:$USER /var/www/html/photos
sudo chmod -R 755 /var/www/html/photos
```

> 📁 **Путь:** Фотографии будут храниться в `/var/www/html/photos/`

---

### 6. Настройка Nginx для раздачи фото

#### Создание конфигурационного файла

```bash
sudo nano /etc/nginx/sites-available/flask-tmc
```

#### Конфигурация Nginx

```nginx
server {
    listen 80;
    server_name ваш_IP_или_домен;  # Например, 192.168.100.106

    # Проксирование запросов к Flask-приложению
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Раздача фотографий
    location /photos/ {
        alias /var/www/html/photos/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Статические файлы
    location /static/ {
        alias /home/flask_tmc_app/static/;
    }
}
```

#### Активация конфигурации

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/flask-tmc /etc/nginx/sites-enabled/

# Проверка конфигурации
sudo nginx -t

# Перезапуск Nginx
sudo systemctl restart nginx
```

---

### 7. Запуск приложения

#### Режим разработки

```bash
cd /home/flask_tmc_app
source venv/bin/activate
python3 app.py
```

> ✅ Приложение будет доступно по адресу: `http://192.168.100.106` (или ваш IP/домен)

#### Режим продакшена (рекомендуется)

```bash
# Установка Gunicorn
pip install gunicorn

# Запуск через Gunicorn
gunicorn -w 4 -b 127.0.0.1:5000 app:app
```

> 💡 **Совет:** Для автозапуска создайте systemd service файл.

---

## 🎯 Функциональность

### 📸 Загрузка фотографий

Фотографии загружаются через форму добавления/редактирования ТМЦ.

- **Хранение:** `/var/www/html/photos/`
- **Доступ:** `http://ваш_IP/photos/имя_файла.jpg`
- **Форматы:** JPG, PNG, GIF
- **Обработка:** Автоматическое переименование для безопасности

---

### 👥 Управление отделами

Отделы управляются через базу данных.

**Добавление нового отдела:**
```sql
INSERT INTO `department` (`name`, `code`, `active`) 
VALUES ('Новый отдел', 'НО', 1);
```

После добавления отдел появится в:
- Выпадающем списке в формах добавления/редактирования ТМЦ
- Фильтрах на главной странице

---

### 🔄 История перемещений

Система отслеживает:
- **Перемещения между МОЛ** (через таблицу `move`)
- **Временные выдачи пользователям** (через таблицу `equipment_temp_usage`)
- **Историю комментариев** к каждому ТМЦ

---

### 📊 Статистика

Доступна статистика по:
- Количеству ТМЦ
- Общей стоимости
- Распределению по отделам
- Временным выдачам

**Доступ:**
- **Администратор:** Статистика по всем ТМЦ
- **МОЛ:** Статистика только по своим ТМЦ
- **Пользователь:** Статистика по полученным ТМЦ

---

## 🧩 Структура проекта

```
flask-tmc-app/
├── app.py                      # Главный файл приложения
├── models.py                   # Модели базы данных (SQLAlchemy)
├── .env                        # Переменные окружения (не коммитится)
├── requirements.txt            # Зависимости Python
├── README.md                   # Документация
│
├── static/                     # Статические файлы
│   ├── css/                    # Стили CSS
│   ├── js/                     # JavaScript файлы
│   └── uploads/                 # Символическая ссылка на /var/www/html/photos/
│
└── templates/                  # Шаблоны Jinja2
    ├── base.html               # Базовый шаблон
    ├── index.html              # Главная страница со списком ТМЦ
    ├── add_tmc.html            # Форма добавления ТМЦ
    ├── edit_tmc.html           # Форма редактирования ТМЦ
    ├── info_tmc.html           # Детальная информация о ТМЦ
    ├── my_temp_tmc.html        # Полученные ТМЦ
    ├── my_friends.html         # Список друзей (МОЛ/пользователи)
    ├── stats.html              # Статистика
    └── ...                     # Другие шаблоны
```

---

## 📎 Полезные команды

| Действие | Команда |
|----------|---------|
| **Активировать окружение** | `source venv/bin/activate` |
| **Запустить приложение (dev)** | `python3 app.py` |
| **Запустить приложение (prod)** | `gunicorn -w 4 -b 127.0.0.1:5000 app:app` |
| **Проверить статус MySQL** | `sudo systemctl status mysql` |
| **Перезапустить Nginx** | `sudo systemctl restart nginx` |
| **Проверить конфиг Nginx** | `sudo nginx -t` |
| **Просмотреть логи Flask** | В терминале, где запущен `app.py` |
| **Просмотреть логи Nginx** | `sudo tail -f /var/log/nginx/error.log` |
| **Деактивировать окружение** | `deactivate` |

---

## 🛡️ Безопасность

### ⚠️ Важные рекомендации

- ❗ **Не используйте встроенный сервер Flask в продакшене**
  - Используйте `gunicorn` + `nginx` для продакшена

- 🔐 **Защита конфиденциальных данных**
  - Храните `.env` вне репозитория
  - Добавьте `.env` в `.gitignore`
  - Используйте сильные пароли для MySQL

- 🔒 **Настройка прав доступа**
  - Настройте правильные права на файлы и директории
  - Ограничьте доступ к базе данных

- 🛡️ **Защита от атак**
  - Используйте HTTPS в продакшене
  - Настройте firewall
  - Регулярно обновляйте зависимости

### 📋 Чек-лист безопасности

- [ ] `.env` добавлен в `.gitignore`
- [ ] Сгенерирован надежный `SECRET_KEY`
- [ ] Настроены пароли для MySQL
- [ ] Настроен firewall
- [ ] Используется HTTPS (для продакшена)
- [ ] Регулярно обновляются зависимости
- [ ] Настроен Gunicorn для продакшена

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи приложения
2. Проверьте логи Nginx
3. Убедитесь, что все зависимости установлены
4. Проверьте настройки базы данных

---

## 📄 Лицензия

Проект обратно совместим с [webuseorg3](https://github.com/donpadlo/webuseorg3).

---

**Разработано с ❤️ для учета ТМЦ**
